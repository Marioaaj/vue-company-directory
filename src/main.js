import { createApp } from 'vue'
import '@/style.css'
import router from '@/router'
import App from './App.vue'

createApp(App).use(router).mount('#app')

//When I changed this part in 19:45 of video it stoped working
